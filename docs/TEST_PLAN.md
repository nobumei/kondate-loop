# TEST_PLAN.md — 手動テスト手順

## スモークテスト(5分・毎変更後に必須)
1. index.html を開く → 下部ナビに4タブ(在庫/献立/買い物/レシート)が表示され、コンソールエラーなし。設定タブは下部ナビに無く、ヘッダー☰→「⚙️ 設定」で開く
2. 在庫: FAB+ → 「鶏もも肉/300g/冷蔵/期限=明日」保存 → 期限が近い食材に「あと1日」で出る
3. 献立: マイクボタンの下の「次へ ▶」→ 確認画面→「🎲 この内容で提案」→ 3品出る。鶏もも系レシピが上位に来やすい(期限ボーナス)
4. 「足りない食材を買い物リストへ」→ 買い物タブに遷移し追加されている
5. 買い物: 1品チェック → 「✓ 選択を在庫に追加」→ 在庫タブに増えている
6. レシート: 画像を1枚アップ → サムネイル表示 → タップで拡大
7. 読み上げ: 買い物タブの「🔊 読み上げ」ボタン → 音声が出る(ミュート解除確認)。献立タブに読み上げボタンが無いこと(v1.9で廃止)
8. リロード → 全データが残っている(献立タブは前回の提案結果画面から始まる)

## 回帰テスト(リリース前)
- 在庫の編集・削除 / 期限なし食材が一覧末尾に来る
- レシピ追加(材料改行パース)・編集・削除、seedレシピに削除ボタンが無い
- お気に入り/頻度/評価の保存、「あまり」設定が提案に出にくい
- 「この献立を作った」→ 最終調理日更新 → 直後の再提案で同じ主菜が出にくい
- 買い物曜日トグル、エクスポート→全削除→インポートで復元
- スマホ実機: 375px幅でレイアウト崩れなし、カメラ起動、ホーム画面追加

## 家族同期テスト(v1.1)
1. 端末A: 設定 > 家族で同期 > あいことば生成 > 同期を開始 → 🟢になる
2. 端末B: 同じあいことばを入力 > 同期を開始 → 確認ダイアログ → 端末Aの在庫が現れる
3. 端末Bで買い物リストにチェック → 端末Aをバックグラウンド→復帰 → 反映されている
4. 機内モードで在庫追加 → 🔴表示 → 通信復帰後、次の変更操作で🟢に戻る
5. 同期を解除 → ローカルデータは残る/以後通信しない(DevToolsのNetworkで確認)
6. エクスポートJSONに householdId が含まれていないこと
7. レシート画像が他端末に現れないこと(仕様どおり)

## v1.2 機能テスト
1. 献立提案の料理名タップ → 材料と作り方(番号付き)がモーダル表示される
2. 👍また食べたい → freq='よく'/rating+1、👎イマイチ → freq='あまり'(再提案で出にくくなる)
3. 買い物曜日を水・日に設定 → 「まとめて提案」→ 次の買い物日までの日数分が出る(曜日未設定なら3日分)
4. 仕込みリストに にんじん等が合算表示され、明日までの分=冷蔵/3日目以降を含む=冷凍案内になる
5. 仕込みチェックがリロード後も保持される
6. 「まとめ買いリストへ」→ 重複なく買い物リストに入る

## v1.3 機能テスト
1. レシピの間隔ボタン(3日〜1年)を変更→「◯日に1回」表示が変わり保存される
2. 「作った」直後の再提案で同じ料理が出にくく、間隔経過後に出やすくなる
3. 👍で間隔が1段短く、👎で1段長くなる(トーストに日数表示)
4. v1.2のデータで開いても旧freqが自動変換されてエラーが出ない

## v1.4 レシートOCR(Gemini)テスト(v1.13でキー入力UIを撤去済み。下記はEdge Function経由の挙動に読み替え)
1. 設定タブにGemini APIキー入力欄が無いこと(`document.getElementById('gemini-key')`が`null`)。レシートタブで「🔍 読み取り」→スピナー表示→確認モーダルが開き、抽出品目がチェックON状態で並ぶ(キー設定なしでそのまま動く)
2. (削除。キー保存導線自体が存在しない)
3. 確認モーダルで1品だけチェックを残し、name/qty/unit/location/expiryを編集→「チェックした1品を在庫に追加」→在庫タブに反映され、チェックを外した品は追加されない
4. 0品抽出または解析失敗時は「読み取れませんでした。手動で追加してください。」と表示され、追加ボタンが出ない
5. APIエラー時(fetchをモックして再現): 429→「無料枠の上限に達しました。1分ほど待って再度お試しください。」、401/403→「画像の読み取りに失敗しました(認証エラー)。時間をおいて再度お試しください。」、500→「サーバー側の設定が未完了です。しばらくお待ちください。」がトースト表示される
6. エクスポートJSONに Gemini APIキーが含まれていないこと(`kondate-loop-gemini`は非推奨・エクスポート対象外)

## v1.5 機能テスト(語録刷新・☰メニュー・チュートリアル・正規化辞書)
1. 全画面のトースト・空表示・注記・確認ダイアログに「なの」等の口調が残っていないこと(`grep 'なの' index.html` で該当なしを確認)
2. 在庫タブから「🔊 期限が近い食材を読み上げ」ボタンが消えていること。`speakExpiring` 関数が存在しないこと(献立読み上げ・買い物読み上げは残っていること)
3. ヘッダー右上の☰をタップ→「📖 使い方」「📝 食材辞書」「⚙️ 設定タブへ」の3項目がドロップダウン表示される→メニュー外をタップすると閉じる
4. localStorageを空にして初回起動→使い方チュートリアルが自動表示される→「はじめる」で閉じ、`kondate-loop-tutorial-seen` が保存される→リロードしても再表示されない
5. ☰→「食材辞書」→ユーザー辞書に別名(例: 玉葱→玉ねぎ)を追加→一覧に表示・削除できる(v1.14で組み込み分も含めた全件一覧+検索UIに刷新。詳細はv1.14機能テスト参照)
6. 在庫に「たまねぎ」を登録した状態で「玉ねぎ」を使うレシピを提案→組み込み辞書により在庫ありと判定される(✓表示)
7. ユーザー辞書に別名を登録→在庫名をその別名にして同様にマッチすることを確認
8. レシートOCRの確認モーダルで、抽出名が辞書の別名だった場合に正規名へ初期変換されていること(モーダル上で編集可)

## v1.6 機能テスト(日数×食事選択・在庫チップ手動補正・事前準備集約・献立カレンダー)
1. 献立タブの「まとめ献立と仕込み」で日数を3、朝と夜にチェックを入れて「まとめて提案」→ 3日分×朝/夜の献立が表示され、朝は汁物+主菜1品の軽量構成になる
2. 単発提案・まとめ提案どちらの材料表示も【◯食材名】(緑)/【✗食材名】(赤)のチップになっている
3. 在庫がある食材のチップ【◯...】をタップ→【✗...】に切り替わる→「まとめ買いリストへ」を押すとその食材が買い物リストに追加される(逆に✗→◯にすると追加されない)。この補正はリロードすると消える(保存されない)
4. 複数日にまたがる食材(例: にんじん)が事前準備リストに合計量+使い先の内訳(「◯本は今日の◯◯、◯本は明日の◯◯で使用」のような1行)で表示され、offset2日目以降の分は冷凍案内が付く
5. 開発者ツールでlocalStorageの`kondate-loop-v1`を旧v1.5形式(`plan.days[].ids`配列、`history`キーなし)に書き換えてリロード→ エラーなく起動し、`state.plan.days[0].meals.夜`に旧献立が入り、`state.history`が`[]`で補完されている
6. 献立タブの「この献立を作った」または👍/👎フィードバックを行うと、献立カレンダーの当日セルにドットが付く→ 日付をタップするとその日の献立名一覧(朝/昼/夜)が表示される
7. 献立カレンダーの「前月」「次月」で表示月が切り替わる
8. 履歴に登録済みのレシピを削除した状態でその日をタップ→「(削除済み)」と表示されエラーが出ない

## v1.7 機能テスト(献立提案への要望反映・調理モード)
1. 献立タブの「今日の要望」を空のまま「献立を提案する」→ 従来どおり即座に提案される(DevToolsのNetworkでGeminiへの通信が発生していないこと)
2. (v1.13でキー入力UI自体を撤去。fetchをモックしてEdge Function呼び出し失敗を再現→ 該当エラーメッセージがトースト表示され、従来ロジックで提案される(コンソールエラーは出ない))
3. 要望を入力して提案→ Gemini呼び出し中は「⏳ AIが考え中…」表示→ 提案結果の下に「💡 AIの提案理由: ...」が表示される。まとめ提案でも同様
4. (開発確認)Geminiが存在しないレシピidを返した場合、そのidは無視され、従来ロジックで該当カテゴリが補完されること(不正idでエラーにならない)
5. Gemini APIエラー(429/401・403/500/その他)時は該当メッセージがトースト表示され、従来ロジックにフォールバックする
6. 要望・AIの提案理由はページをリロードすると消えている(stateに保存されないこと)
7. 音声入力非対応ブラウザ(または`window.SpeechRecognition`/`webkitSpeechRecognition`が存在しない環境)では🎤ボタンが表示されないこと。対応環境では🎤タップで音声認識が開始し、認識結果が要望欄に入ること
8. 作り方が登録されているレシピの詳細で「🔊 調理モード」ボタンが表示され、未登録レシピでは表示されないこと
9. 調理モードを開くと「工程 1/N」と現在の工程が表示され、読み上げが開始される。「次へ」「前へ」「もう一回」のタップがすべて即座に効くこと(音声認識の状態に関わらず)
10. 最終工程の「次へ」後は「完了」表示になり、ボタンが「閉じる」に変わる。閉じるとモーダルが閉じる
11. 調理モード表示中に背景タップ/「閉じる」ボタン/最終工程後の「閉じる」のいずれで閉じても、`speechSynthesis.cancel()`と音声認識の停止が必ず実行されること(閉じた後に音声が流れ続けない)
12. 音声認識対応環境では、読み上げ完了後に自動で待受が始まり、「つぎ/次/OK/オッケー」で次へ、「もう一回/もういちど」で再読み上げ、「もどる/前」で前へ進む。それ以外の発話(誤認識)では何も起きない

## v1.8 機能テスト(チラシOCR特売+スコア加点・レシート集計)
1. 買い物タブ「📰 チラシから特売を取り込む」→ 画像を選択→ Gemini抽出結果の確認リストが開く(チェックON状態)。編集・チェック解除ができ、「チェックした◯件を取り込む」→ 特売リストに反映される。チラシ画像自体がlocalStorage(`kondate-loop-v1`)に保存されていないこと(DevToolsでstate.dealsにdataUrl等の画像データが含まれないことを確認)
2. 特売リストの品目名と、在庫に近い名前の買い物リスト品目に「特売 ¥◯◯」バッジが付く(normalizeName一致。例: 特売名「たまねぎ」→買い物リスト「玉ねぎ」でも一致)
3. 献立提案で特売食材を含むレシピのスコアが底上げされ、再提案で選ばれやすくなる(同一レシピをscoreRecipeで比較すると特売ありの方が高スコア)
4. 提案画面の【◯/✗】チップで、✗かつ特売中の食材のチップに「特売」の表示が追加される。在庫があり◯表示の食材には付かない
5. 特売の期限(validUntil)が今日より前のものは特売リスト・バッジ・スコア加点のいずれにも出てこない(非表示なだけでstate.dealsのデータ自体は残る)
6. 特売リスト先頭の「🧹 期限切れを削除」→ 確認ダイアログ→ 期限切れの特売のみ削除される(在庫・買い物リスト・レシピ等は変更されない)。期限切れが0件のときはボタンが表示されない
7. (v1.13でキー入力UIを撤去。fetchをモックしてAPIエラー(429/401・403/500)を再現→ 該当メッセージがトースト表示される)
8. 旧形式データ(`deals`キーなし)を開いてもエラーが出ず、`state.deals`が`[]`で補完される
9. レシートの「🔍 読み取り」→ 確認モーダルに各品目のジャンル(野菜/肉/魚/乳製品/主食/調味料/飲料/菓子/日用品/その他)が表示され、食品ジャンルはチェックON、日用品/その他は既定OFFになっている
10. 確認モーダルで「在庫に追加」を実行→ チェックした品だけが在庫に追加され、チェックの有無に関わらず全抽出品目+レシート合計金額(total)がそのレシートのitems/totalとして保存される
11. レシートタブ「📊 集計」で月を選択→ その月の月間総額(レシートにtotalがあれば優先、なければitems合計)・ジャンル別内訳(金額降順の横バー表示)・レシート枚数が表示される。複数月にレシートがある場合、月選択に両方が表示され切り替えられる
12. 読み取り→在庫に追加を実行していない(itemsが無い)レシートは集計の月選択・金額に含まれないこと。集計対象がない月は「集計できるレシートがありません」の注記が出る

## v1.9 機能テスト(献立タブUX刷新: マイク起点フロー・まとめ提案の格納式UI・日別アコーディオン・多様性)
1. 献立タブを開く→ 単発提案が「(a)マイクボタン中心の入力画面」で始まり、まとめ提案・カレンダー・レシピ一覧はその下にあること。まとめ提案は「📅 数日分をまとめて提案(タップで開く)」の小さいボタンだけが見え、日数・朝昼夜・生成ボタン等は隠れていること(初回・要望なし状態)
2. (a)入力画面でテキスト欄に要望を入力して「次へ ▶」→ (b)確認画面に入力内容がそのまま表示され、「🎲 この内容で提案」「◀ 話し直す・修正する」の2ボタンが出る
3. (b)で「◀ 話し直す・修正する」→ (a)入力画面に戻り、直前のテキストが編集できる状態で残っている
4. (a)で要望を空のまま「次へ ▶」→ (b)に「(要望なし・おまかせで提案します)」と表示される→「🎲 この内容で提案」→ 従来ロジックで即座に(c)結果画面に3品出る(Geminiは呼ばれない)
5. 音声入力対応ブラウザでは(a)の🎤ボタンをタップすると認識が始まり(🎙️表示)、認識結果がテキスト欄に反映されると自動で(b)確認画面に進む。非対応ブラウザでは🎤タップ時に「このブラウザは音声入力に対応していません。下のテキスト欄に入力してください。」がトースト表示され、テキスト欄にフォーカスが移る(クラッシュしない)
6. (c)結果画面の「🎤 話し直して再提案する」→ (a)入力画面に戻る。「🛒 足りない食材を買い物リストへ」「✅ この献立を作った」は従来どおり動作する
7. 献立タブを再読み込み→ 前回提案済みなら(c)結果画面から始まる(前回の提案内容が残っている)。未提案なら(a)入力画面から始まる
8. 「📅 数日分をまとめて提案(タップで開く)」をタップ→ 日数・朝昼夜チェック・要望入力・「まとめて提案」ボタンが展開される。もう一度タップすると閉じ、ボタン文言が「開く」⇄「閉じる」で切り替わる
9. まとめて提案を実行→ 結果は「🔪事前準備リスト」が最上部に表示され、その下に日ごとのカード(「今日 ▲」のように既定で今日だけ開いている)が並ぶ。他の日は「◀ 明日 ▼」のように閉じている
10. 閉じている日のヘッダーをタップ→ その日の献立(朝/昼/夜・材料チップ)が開く→ もう一度タップで閉じる。仕込みチェックを1つ入れても、開閉していた日の状態が維持される(全部閉じ直されない)
11. まとめ提案済みの状態でリロード→ まとめ提案セクションが自動で展開されている(データが隠れたままにならない)
12. 献立を複数回(要望なしで5回程度)提案し直す→ 主菜と副菜・汁物の主タンパク源(肉/魚/卵/大豆など)が同じ提案内で重なりにくいこと(まったく同じ組み合わせにはならなくてよいが、露骨に同じ食材が連発しないことを目視確認)
13. まとめて提案(3日・朝昼夜)を複数回実行→ 同じ主菜(全く同一レシピ)が隣接する日に連続しにくいこと
14. 献立タブ・レシピ一覧のどこにも「🔊 献立を読み上げ」ボタンが無いこと。`speakMenu` 関数がコンソールで `undefined` であること(買い物タブの読み上げ・調理モードの読み上げは引き続き動作すること)

## v1.10 機能テスト(チュートリアル3枚スライド・☰設定集約・買い物記録削除・調理モード分量読み上げ)
1. localStorageを空にして初回起動→ 使い方チュートリアルが自動表示され、1枚目(🧾レシートを撮る)から始まる。「◀ 戻る」は1枚目では非表示、「次へ ▶」で2枚目(🍽献立を立てて買い物リストへ)→3枚目(👨‍👩‍👧家族と共有)に進み、ドットインジケータが現在位置に連動する。3枚目だけボタンが「はじめる」になり、押すとモーダルが閉じて`kondate-loop-tutorial-seen`が保存される
2. 2〜3枚目で「◀ 戻る」を押すと前のスライドに戻る。タッチ環境では本文を左右スワイプしてもスライドが送り/戻りできる
3. スマホ幅(375×667程度)でチュートリアルを開く→ モーダルの高さが画面に収まり(`max-height:85vh`程度)、本文が長くても下部の「戻る/次へ・はじめる」ボタンは常に画面内に表示されタップできる。ブラウザの開発者ツールでビューポートを375×667にしてボタンの座標が画面内(bottom<viewport高さ)であることを確認
4. ヘッダー☰→ 表示される項目が「📖 使い方」「📝 食材辞書」「⚙️ 設定」の3つ。「⚙️ 設定」タップで設定画面が開く(page-settingsが表示される)
5. 下部ナビが4つ(在庫/献立/買い物/レシート)で、⚙️設定タブが無いこと。4タブすべてタップしてページ切り替え・アクティブ表示・FABの表示/非表示(在庫・レシートのみ表示)が従来どおり動くこと
6. 設定画面に「🧮 買い物記録」セクション(「＋ 今日買い物した」ボタン含む)が存在しないこと。`document.getElementById('shopLogSummary')`が`null`であること。`logShopping`/`renderShopLog`関数がコンソールで`undefined`であること
7. 旧データ(`state.settings.shoppingLog`に日付が入った状態)をインポート→ エラーなく読み込め、エクスポートJSONにも`shoppingLog`がそのまま残っていること(データ自体は非破壊)
8. `renderAll()`がエラー無く実行される(在庫追加・削除など通常操作後も画面が正しく再描画される)
9. 作り方が登録済みのレシピで「🔊 調理モード」を開く→ 材料がある場合は最初に「材料」カードが表示され、読み上げも「材料は、〇〇大さじ2、△△300グラム…です。」のように分量付きで再生される。「次へ」で工程1に進み、工程1で「前へ」を押すと材料カードに戻れる(それより前には戻れない)
10. 材料が空のレシピ(手動でings未入力のレシピを作成)で調理モードを開く→ 材料カードは出ず工程1から始まる
11. 豚の生姜焼きの材料表示・調理モードで「しょうが」が「10g」(曖昧な「1かけ」ではない)として表示・読み上げられること

## v1.11 機能テスト(レシピOCR取り込み・検索リンク直リンク化)
1. (v1.13でキー入力UI自体を撤去。献立タブ「📷 本・画面からレシピを取り込む」→ 画像を選択→ キー設定なしでそのままGemini呼び出しが行われること)
2. 料理本の写真を選択→ スピナー表示→ 抽出結果の確認モーダルが開き、レシピごとに名前(編集可)・区分(主菜/副菜/汁物セレクト)・調理時間・材料(name/qty/unit、行の追加・削除可)・作り方(1行1手順、行の追加・削除可)がチェックON状態で表示される
3. 画像に複数のレシピが写っている場合、確認モーダルにレシピごとのカードが複数並ぶこと
4. 1件だけチェックを残して他を外し、残した1件の名前・材料・作り方を編集して「チェックした1件をレシピに追加」→ レシピ一覧・件数表示に反映され、チェックを外したレシピは追加されないこと。追加されたレシピは`seed:false`で削除ボタンが表示され、献立提案の候補にも出ること
5. 材料が1つも読み取れなかったレシピは自動的にスキップされ、「材料が読み取れなかった◯件はスキップしました。」がトースト表示されること。名前はあるが材料が空の場合も同様にスキップされる
6. 0件抽出または解析失敗時は「読み取れませんでした。」の空表示になり、追加ボタンが表示されないこと
7. 確認モーダルで全レシピのチェックを外した状態で追加ボタンを押すと「追加できるレシピがありません。」のトーストが出て、`state.recipes`が変化せずモーダルも閉じないこと
8. APIエラー時(429/401・403/500/その他)は該当メッセージがトースト表示される(レシートOCRと同じ文言)。エクスポートJSONにGemini APIキーが含まれないこと(既存キーと同じ端末ローカル方針)
9. レシピ詳細(タップ→作り方)の「もっと詳しく」リンクが `https://www.kurashiru.com/search?query=<レシピ名>` と `https://delishkitchen.tv/search?q=<レシピ名>` になっていること(材料は付与しない、target=_blank・rel=noopener を維持)

## v1.13 機能テスト(Geminiキーのクライアント撤去・Edge Functionプロキシ統一)
1. 設定タブ(☰→⚙️設定)を開く→「🔍 レシートOCR(Gemini)」カード自体が存在しないこと。「家族で同期」の下に直接「💾 データ」セクションが続くこと(視覚的な隙間・崩れが無いこと)
2. `document.getElementById('gemini-key')` が `null`、`typeof loadGeminiKey` / `typeof saveGeminiKey` / `typeof GEMINI_KEY_STORAGE` がいずれも `undefined` であること(コンソールで確認、`renderAll()`実行時にエラーが出ないこと)
3. `window.fetch` を一時的にモックして `callGemini()` を直接呼び出す→ リクエスト先が `${SYNC_URL}/functions/v1/gemini`、ヘッダーが `syncHeaders()`(apikey/Authorization/Content-Type)、bodyが `{model, contents, generationConfig}` のJSONであること
4. レシートOCR・チラシOCR・レシピOCR・献立生成(要望入力あり)のいずれを実行しても、DevTools Networkで `generativelanguage.googleapis.com` への直接リクエストが発生せず、`{SYNC_URL}/functions/v1/gemini` へのPOSTのみが発生すること
5. `callGemini` のモックで各HTTPステータスを再現→ 429「無料枠の上限に達しました。1分ほど待って再度お試しください。」、401/403「画像の読み取りに失敗しました(認証エラー)。時間をおいて再度お試しください。」、500「サーバー側の設定が未完了です。しばらくお待ちください。」、その他「通信エラーが発生しました(HTTP xxx)。」がそれぞれのトーストに表示されること
6. 献立生成で要望を入力し、`callGemini`が失敗するようモックした状態で提案を実行→ 例外がconsole.errorに記録されつつ、通常の3品提案(pickBest経由)にフォールバックし、`suggest()`自体は例外を投げずに完了すること
7. レシート/献立の各OCR注記文言に「設定タブでGemini APIキー登録が必要」の文言が残っていないこと(`grep 'Gemini APIキー登録' index.html` で該当なし)

## v1.12 機能テスト(献立クリア・買い物一括操作・食材辞書リネーム/拡充)
1. 献立タブで単発提案を実行→結果画面(c)が表示された状態で「🗑 クリア」を押す→ 提案結果が消え、(a)入力画面に戻る。リロードしても結果が復活しない(再提案するまで(a)から始まる)ことを確認
2. 買い物リストに未チェックの品を複数追加した状態でタブを開く→ 一括操作バーに「☑️ すべて選択」ボタンが表示される→ タップ→ 全品にチェックが入り、ボタン表示が「☐ 選択を解除」に変わる→ もう一度タップ→ 全品のチェックが外れ、ボタン表示が「☑️ すべて選択」に戻る
3. 買い物リストが空の状態で「☑️ すべて選択」を押す→「買い物リストは空です。」トーストが出て何も起きない
4. 数品にチェックを入れ「✓ 選択を在庫に追加」→ チェックした品だけが在庫タブに反映され(場所=冷蔵、期限=空欄、数量/単位は引き継ぎ)、買い物リストから消える。チェックしていない品は買い物リストに残る
5. 何もチェックしていない状態で「✓ 選択を在庫に追加」→「選択した品がありません。」トーストが出て在庫・買い物リストとも変化しない
6. 数品にチェックを入れ「🗑 選択を削除」→ 件数入りの確認ダイアログが出る→ OKでチェックした品だけが買い物リストから削除される。キャンセルすると削除されない
7. 何もチェックしていない状態で「🗑 選択を削除」→「選択した品がありません。」トーストが出て確認ダイアログは出ない
8. ヘッダー☰の項目・食材辞書モーダルのタイトルがいずれも「食材辞書」表記になっていること(「食材名の辞書」という文字列が画面上に残っていないこと)
9. 在庫に「玉ねぎ」を登録した状態で材料に「たまねぎ」を使うレシピを提案→ 在庫ありと判定される(✓表示)。逆に在庫「ニンジン」・材料「にんじん」でも同様にヒットする
10. ☰→「食材辞書」→(v1.14でUIを全件一覧+検索に刷新。件数表示の文言は変わったが128組・63食材という組み込みデータ自体は不変。詳細はv1.14機能テスト参照)
11. 開発者ツールのコンソールで `normalizeName('人参')` → `'にんじん'`、`normalizeName('鶏もも')` → `'鶏もも肉'`、`normalizeName('たまご')` → `'卵'` になることを確認(ひらがな優先・肉/卵は自然表記のまま)
12. 既存データ(在庫に漢字表記「大根」、レシピ材料に「だいこん」等)を持つ状態でリロード→ 保存済みの表記自体は書き換わらず、提案画面の在庫チップ判定(inStock)は引き続き正しく◯/✗判定されること(非破壊の確認)

## v1.14 機能テスト(卵チップ誤✗バグ修正・食材辞書の全件一覧+検索+追加・食材ごとの賞味期限デフォルト)
1. 在庫を空にしてFAB+→「食材名」欄に手動で「たまご」(ひらがな、装飾なし)と入力→期限を空欄のまま保存→在庫一覧に「卵」として保存され(正規化済み)、期限に今日+14日程度の日付が自動で入っていること
2. 1の状態で献立タブの単発提案を実行(または `ingChips()` を含む卵材料のレシピ、例: 卵スープ)→ 卵の材料チップが【◯卵】(緑)になっていること。同様に在庫名を「卵」「玉子」、材料名を「卵」「溶き卵」「たまご」の総当たり9パターンすべてで◯になることを確認(コンソールで `inStock('溶き卵')` 等を直接呼んでも良い)
3. 在庫に「しおから」を追加した状態でコンソールから `inStock('塩')` → `false`(短い正規名の誤爆防止。以前のロジックでは`true`になっていた)
4. ヘッダー☰→「食材辞書」→ 検索欄なしで開くと63/63件(組み込み)+ユーザー登録分の件数が表示され、一覧に正規名ごとに「組込み: 別名, 別名…」の形でグルーピング表示されること。検索欄に「卵」と入力→「卵」の1件だけに絞り込まれること
5. 食材辞書モーダル右上の「＋」→ 別名/正規名の追加フォームが開く→ 別名「アボカド豆腐」正規名「豆腐」を追加→ 一覧の「豆腐」行に「ユーザー: アボカド豆腐 🗑」が組込み別名と別に表示される→ 🗑で削除できる
6. 食材辞書の「卵」行にある冷蔵/冷凍/常温の数値入力を変更(例: 冷凍を5に)→ 値がすぐ保存される(リロードしても保持)。入力を空にする→ その保存場所だけ組み込みデフォルト値に戻る(表示上は組み込み値または全体フォールバック値に戻る)
7. レシートOCRで「🔍読み取り」→ Gemini抽出結果にexpiryGuessが無い品目の期限初期値が、賞味期限デフォルトに基づく日付になっていること(expiryGuessがある品目はそちらが優先されること)
8. 買い物タブで品目をチェックして「✓ 選択を在庫に追加」→ 追加された在庫の期限に、その食材の賞味期限デフォルトに基づく日付が自動で入っていること(以前は空欄だった)
9. 開発者ツールでlocalStorageの`kondate-loop-v1`から`settings.shelfLife`キーを削除してリロード→ エラーなく起動し`state.settings.shelfLife`が`{}`で補完されていること(既存の在庫・normDict等は変化しないこと)
10. JS構文チェック(`node --check`相当)・ブラウザのコンソールにアプリ由来のエラーが出ていないこと

## 端末マトリクス
iOS Safari(最優先) / Android Chrome / PC Chrome
